import React, { Component } from "react";

// import { Container } from './styles';

export default class InputBox extends Component {
    render() {
        return (
            <textarea className="code-area" onChange></textarea>
        );
    }
}